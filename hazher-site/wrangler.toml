# Cloudflare Pages — hazher.no
# Root directory in dashboard should be hazher-site (if monorepo) or this folder.

name = "hazher"
compatibility_date = "2025-03-01"
pages_build_output_dir = "."

# Create a KV namespace in Cloudflare → Workers & Pages → KV,
# then paste the ID below (or bind HAZHER_HQ in Pages → Settings → Functions → KV bindings).
# [[kv_namespaces]]
# binding = "HAZHER_HQ"
# id = "YOUR_KV_NAMESPACE_ID"
